# 예시: iOS 빌드를 위한 기본 codemagic.yaml 구조
workflows:
  ios-workflow:
    name: iOS Deployment Workflow
    max_build_duration: 60
    instance_type: mac_mini_m1
    triggering:
      events:
        - push
      branches:
        - main
    scripts:
      - name: Say Hello
        script: echo "Hello Codemagic!"